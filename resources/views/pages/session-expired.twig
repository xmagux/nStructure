{% extends 'layouts/auth.twig' %}

{% block content %}
<div class="auth-head"><span class="eyebrow">nStructure</span><h1>{{ t('auth.session_expired_title') }}</h1></div>
<div class="auth-form">
    <a class="button button-primary button-block" href="{{ login_url }}">{{ t('auth.session_expired_action') }}</a>
</div>
{% endblock %}
